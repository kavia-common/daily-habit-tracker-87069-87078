import type { NextConfig } from "next";

/**
 * App uses the App Router (src/app). Using default Next.js 15 configuration.
 * If you later introduce a pages/ directory, ensure compatibility as per Next.js docs.
 */
const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
